# School Voting Demo

```bash
npm install
npm run dev
```

Routes:

- `/boy` for Head Boy and Deputy Head Boy
- `/girl` for Head Girl and Deputy Head Girl

Admin PIN for the demo: `2905`

Candidate photos live in `public/contenders`.

Symbol images live in `public/symbols`.

Update names, symbols, and image paths in `src/data/contenders.js`.
